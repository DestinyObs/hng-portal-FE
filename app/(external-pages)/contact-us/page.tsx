import ContactUsPage from '@/components/contact-us/page';

export const metadata = {
  title: 'Contact Us | HNG Connect',
  description: 'Contact us at HNG Connect.',
};

export default function Page() {
  return <ContactUsPage />;
}
